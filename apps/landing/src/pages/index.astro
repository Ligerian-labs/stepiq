---
import Layout from "../layouts/Layout.astro";

const APP_URL = import.meta.env.PUBLIC_APP_URL || "http://localhost:5173";

const howItWorks = [
  {
    step: "01",
    title: "Define",
    description:
      "Build your pipeline in the editor or import YAML. Set steps, models, variables, and output format.",
  },
  {
    step: "02",
    title: "Trigger",
    description:
      "Run on demand, via API, webhook, or cron (plan-dependent). Track execution live with per-step status and stream updates.",
  },
  {
    step: "03",
    title: "Deliver",
    description:
      "Return output via API response or send to outbound webhooks with signing and retries. More delivery targets are rolling out.",
  },
];

const coreFeatures = [
  {
    icon: `<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="3"/><path d="M12 1v6m0 6v6m-7-7h6m6 0h6"/><path d="M19.07 4.93l-4.24 4.24M9.17 14.83l-4.24 4.24M4.93 4.93l4.24 4.24M14.83 14.83l4.24 4.24"/></svg>`,
    title: "Model-agnostic",
    desc: "Route each step to the right model provider with unified retries, auth, and usage tracking.",
  },
  {
    icon: `<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M7 8h10M7 12h4m1 8l-4-4H5a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v8a2 2 0 01-2 2h-3l-4 4z"/></svg>`,
    title: "Variable interpolation",
    desc: "Reference previous outputs, user inputs, secrets, and pipeline metadata with Handlebars-like syntax.",
  },
  {
    icon: `<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="1 4 1 10 7 10"/><path d="M3.51 15a9 9 0 105.69-9.51L1 10"/></svg>`,
    title: "Retries & conditions",
    desc: "Built-in retry with backoff. Conditional branching lets you loop or skip steps based on output quality.",
  },
  {
    icon: `<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>`,
    title: "Cron scheduling",
    desc: "Schedule pipelines with cron expressions. Timezone-aware, with distributed locking for reliability.",
  },
];

const differentiators = [
  {
    icon: `<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="3"/><path d="M12 1v6m0 6v6m-7-7h6m6 0h6"/><path d="M19.07 4.93l-4.24 4.24M9.17 14.83l-4.24 4.24M4.93 4.93l4.24 4.24M14.83 14.83l4.24 4.24"/></svg>`,
    title: "Model-agnostic by default",
    desc: "Use the best model for each step. Mix Anthropic, OpenAI, Meta Llama, and others in one workflow.",
    proofLabel: "See supported models",
    proofHref: "/docs/models-pricing",
    external: false,
  },
  {
    icon: `<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="11" width="18" height="11" rx="2" ry="2"/><path d="M7 11V7a5 5 0 0110 0v4"/></svg>`,
    title: "Privacy-first, security-first",
    desc: "Secrets are encrypted, traffic is protected, and data handling is explicit. Security controls are built into the core platform.",
    proofLabel: "Read privacy & security details",
    proofHref: "/privacy",
    external: false,
  },
  {
    icon: `<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"/></svg>`,
    title: "Open source and auditable",
    desc: "The platform code is public so teams can verify behavior, self-host strategy, and integrate transparently.",
    proofLabel: "View source on GitHub",
    proofHref: "https://github.com/Ligerian-labs/automate",
    external: true,
  },
];

const secondaryFeatures = [
  {
    icon: `<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M2 12s3-7 10-7 10 7 10 7-3 7-10 7-10-7-10-7z"/><circle cx="12" cy="12" r="3"/></svg>`,
    title: "Real-time streaming",
    desc: "Watch each step execute live via SSE. See token counts, costs, and outputs as they stream in.",
  },
  {
    icon: `<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="11" width="18" height="11" rx="2" ry="2"/><path d="M7 11V7a5 5 0 0110 0v4"/></svg>`,
    title: "Encrypted secrets",
    desc: "Store API keys and tokens with AES-256-GCM encryption. Reference them safely in prompts via {{env.xxx}}.",
  },
  {
    icon: `<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M14 2H6a2 2 0 00-2 2v16a2 2 0 002 2h12a2 2 0 002-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/></svg>`,
    title: "YAML import/export",
    desc: "Define pipelines in YAML or JSON. Import, export, and version control your workflows alongside your code.",
  },
  {
    icon: `<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="1" x2="12" y2="23"/><path d="M17 5H9.5a3.5 3.5 0 000 7h5a3.5 3.5 0 010 7H6"/></svg>`,
    title: "Cost tracking",
    desc: "Per-step token counts and costs. Get estimates before running. No surprise bills.",
  },
  {
    icon: `<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M10 13a5 5 0 007.54.54l3-3a5 5 0 00-7.07-7.07l-1.72 1.71"/><path d="M14 11a5 5 0 00-7.54-.54l-3 3a5 5 0 007.07 7.07l1.71-1.71"/></svg>`,
    title: "API & webhooks",
    desc: "Trigger pipelines programmatically. Unique webhook URLs per pipeline. Full REST API with scoped keys.",
  },
  {
    icon: `<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 20h9"/><path d="M16.5 3.5a2.121 2.121 0 013 3L7 19l-4 1 1-4L16.5 3.5z"/></svg>`,
    title: "Version history",
    desc: "Every edit creates an immutable snapshot. Roll back to any version. Compare definitions side by side.",
  },
];

const pricing = [
  {
    name: "Starter",
    monthly: "19€",
    yearly: "205€",
    periodMonthly: "/month",
    periodYearly: "/year",
    caption: "2,000 credits included",
    items: [
      "10 pipelines",
      "10 steps per pipeline",
      "100 runs/day",
      "All core models",
      "Cron scheduling",
      "API access",
    ],
    cta: "Start starter",
    featured: false,
    paid: true,
  },
  {
    name: "Pro",
    monthly: "49€",
    yearly: "529€",
    periodMonthly: "/month",
    periodYearly: "/year",
    caption: "8,000 credits included",
    items: [
      "Unlimited pipelines",
      "20 steps per pipeline",
      "500 runs/day",
      "All models",
      "Cron scheduling",
      "YAML import/export",
      "Webhooks & API access",
      "Priority execution",
    ],
    cta: "Start free trial",
    featured: true,
    paid: true,
  },
  {
    name: "Enterprise",
    monthly: "Custom",
    yearly: "Custom",
    periodMonthly: "",
    periodYearly: "",
    caption: "Custom credit allocation",
    items: [
      "Everything in Pro",
      "SSO / SAML",
      "Dedicated workers",
      "SLA guarantee",
      "Custom model endpoints",
      "Priority support",
    ],
    cta: "Contact sales",
    featured: false,
    paid: false,
  },
];

const pipelineSteps = [
  {
    icon: `<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="7"/><path d="m21 21-4.3-4.3"/></svg>`,
    title: "Research",
    model: "claude-sonnet-4",
    active: true,
    progress: 100,
  },
  {
    icon: `<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="7"/><circle cx="12" cy="12" r="3"/></svg>`,
    title: "Pick Topic",
    model: "gpt-4o",
    active: true,
    progress: 70,
  },
  {
    icon: `<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 20h9"/><path d="M16.5 3.5a2.12 2.12 0 0 1 3 3L7 19l-4 1 1-4Z"/></svg>`,
    title: "Write Draft",
    model: "claude-sonnet-4",
    active: false,
    progress: 0,
  },
  {
    icon: `<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m9 12 2 2 4-4"/><circle cx="12" cy="12" r="9"/></svg>`,
    title: "Quality Review",
    model: "claude-sonnet-4",
    active: false,
    progress: 0,
  },
];

const codeContent = `name: weekly-blog-generator
version: 1

variables:
  language: "fr"
  tone: "direct, pragmatic"

steps:
  - id: research
    model: claude-sonnet-4
    output_format: json
    retry:
      max_attempts: 2

  - id: write_draft
    model: claude-sonnet-4
    output_format: markdown`;

const structuredData = [
  {
    "@context": "https://schema.org",
    "@type": "Organization",
    name: "stepIQ",
    url: "https://stepiq.sh",
    sameAs: ["https://github.com/Ligerian-labs/automate"],
  },
  {
    "@context": "https://schema.org",
    "@type": "WebSite",
    name: "stepIQ",
    url: "https://stepiq.sh",
  },
  {
    "@context": "https://schema.org",
    "@type": "SoftwareApplication",
    name: "stepIQ",
    applicationCategory: "DeveloperApplication",
    operatingSystem: "Web",
    offers: {
      "@type": "Offer",
      price: "0",
      priceCurrency: "EUR",
    },
    description:
      "Model-agnostic AI pipeline orchestration with usage-based credits and built-in cost tracking.",
  },
];
---

<Layout
  title="stepIQ — Build AI pipelines. Ship without infrastructure."
  description="stepIQ is a model-agnostic AI pipeline platform with built-in security, open-source transparency, and usage-based pricing."
  path="/"
  keywords={["AI pipeline", "model agnostic", "open source AI workflows", "AI cost estimation", "stepIQ"]}
  structuredData={structuredData}
>
  <main class="landing">
    <header class="header">
      <div class="brand">
        <div class="logo-icon">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="7" height="7" rx="1.5"/><rect x="14" y="14" width="7" height="7" rx="1.5"/><path d="M7 10v4a3 3 0 0 0 3 3h4"/><circle cx="17.5" cy="6.5" r="3.5"/></svg>
        </div>
        <span>stepIQ</span>
      </div>
      <nav class="nav-links">
        <a href="#why-stepiq">Why stepIQ</a>
        <a href="#features">Features</a>
        <a href="#pricing">Pricing</a>
        <a href="/docs">Docs</a>
        <a href="#templates">Templates</a>
      </nav>
      <div class="actions">
        <a href={`${APP_URL}/login`} class="ghost-link">Log in</a>
        <a href={`${APP_URL}/register`} class="solid-link">Get Started</a>
      </div>
      <button type="button" class="mobile-menu-btn" aria-label="Open menu" id="mobile-menu-open">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="3" y1="6" x2="21" y2="6"/><line x1="3" y1="12" x2="21" y2="12"/><line x1="3" y1="18" x2="21" y2="18"/></svg>
      </button>
    </header>

    <div class="mobile-nav" id="mobile-nav">
      <div class="mobile-nav-header">
        <div class="brand">
          <div class="logo-icon">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="7" height="7" rx="1.5"/><rect x="14" y="14" width="7" height="7" rx="1.5"/><path d="M7 10v4a3 3 0 0 0 3 3h4"/><circle cx="17.5" cy="6.5" r="3.5"/></svg>
          </div>
          <span>stepIQ</span>
        </div>
        <button type="button" class="mobile-nav-close" aria-label="Close menu" id="mobile-menu-close">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
        </button>
      </div>
      <a href="#why-stepiq" class="mobile-nav-link">Why stepIQ</a>
      <a href="#features" class="mobile-nav-link">Features</a>
      <a href="#pricing" class="mobile-nav-link">Pricing</a>
      <a href="/docs" class="mobile-nav-link">Docs</a>
      <a href="#templates" class="mobile-nav-link">Templates</a>
      <div class="mobile-nav-cta">
        <a href={`${APP_URL}/login`} class="outline-link">Log in</a>
        <a href={`${APP_URL}/register`} class="solid-link">Get Started</a>
      </div>
    </div>

    <section class="hero">
      <div class="beta-badge hero-reveal hero-reveal-1"><span class="dot"></span> Now in public beta</div>
      <div class="hero-text-wrap hero-reveal hero-reveal-2">
        <h1>Build AI pipelines.<br />Ship without infrastructure.</h1>
        <p class="hero-sub">
          Chain models, prompts and logic into multi-step workflows.<br />
          Trigger via API, cron, or one click. Model-agnostic, privacy-first, and open source.
        </p>
      </div>
      <div class="hero-actions hero-reveal hero-reveal-3">
        <a href={`${APP_URL}/register`} class="solid-link">Start building free <span class="arrow-icon">→</span></a>
        <a href="/docs" class="outline-link">View documentation</a>
      </div>

      <article class="pipeline-mockup hero-reveal hero-reveal-4" id="docs">
        <div class="mock-head">
          <strong class="mock-title">weekly-blog-generator</strong>
          <span class="status"><span class="status-dot"></span> Running</span>
        </div>
        <div class="steps">
          {pipelineSteps.map((step, i) => (
            <>
              {i > 0 && <div class="arrow">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
              </div>}
              <div class={`step ${step.active ? 'step-active' : 'step-pending'} ${i === 0 ? 'step-first' : ''} ${i === 1 ? 'step-second' : ''}`}>
                <span class="step-icon" set:html={step.icon} />
                <span class="step-title">{step.title}</span>
                <span class="step-model">{step.model}</span>
                <div class="step-bar">
                  <div class={`step-bar-fill ${step.active ? 'bar-accent' : 'bar-muted'}`} style={`width: ${step.active ? step.progress + '%' : '0%'}`}></div>
                </div>
              </div>
            </>
          ))}
        </div>
      </article>

      <div class="trust hero-reveal hero-reveal-5">
        <span class="trust-label">Works with</span>
        <span class="trust-sep"></span>
        <span>Anthropic</span>
        <span>OpenAI</span>
        <span>Meta Llama</span>
        <span>Mistral</span>
      </div>
    </section>

    <section class="section" id="why-stepiq">
      <div class="section-header reveal-on-scroll">
        <p class="kicker">WHY WE'RE DIFFERENT</p>
        <h2>Built for teams who need control</h2>
        <p class="section-sub">Not another black-box wrapper: choose models freely, keep security standards high, and build on open code.</p>
      </div>
      <div class="differentiators-grid">
        {differentiators.map((item) => (
          <article class="card differentiator-card reveal-on-scroll">
            <div class="feat-icon" set:html={item.icon} />
            <h3>{item.title}</h3>
            <p>{item.desc}</p>
            <a
              href={item.proofHref}
              class="proof-link"
              target={item.external ? "_blank" : undefined}
              rel={item.external ? "noopener noreferrer" : undefined}
            >
              {item.proofLabel} <span aria-hidden="true">→</span>
            </a>
          </article>
        ))}
      </div>
    </section>

    <section class="section">
      <div class="section-header reveal-on-scroll">
        <p class="kicker">HOW IT WORKS</p>
        <h2>From YAML to production<br />in three steps</h2>
        <p class="section-sub">Define once, trigger from your stack, and ship outputs with production-ready execution flow.</p>
      </div>
      <div class="card-grid three">
        {howItWorks.map((item) => (
          <article class="card reveal-on-scroll">
            <div class="step-pill">{item.step}</div>
            <h3>{item.title}</h3>
            <p>{item.description}</p>
          </article>
        ))}
      </div>
    </section>

    <section class="section" id="features">
      <div class="section-header reveal-on-scroll">
        <p class="kicker">PIPELINE AS CODE</p>
        <h2>Everything you need to<br />orchestrate AI workflows</h2>
      </div>
      <div class="features-layout">
        <div class="code-card reveal-on-scroll">
          <div class="code-header">
            <div class="code-dots">
              <span class="code-dot red"></span>
              <span class="code-dot yellow"></span>
              <span class="code-dot green"></span>
            </div>
            <span class="code-filename">pipeline.yaml</span>
          </div>
          <pre class="code-body"><code set:html={codeContent
            .replace(/&/g, '&amp;')
            .replace(/</g, '&lt;')
            .replace(/>/g, '&gt;')
            .replace(/^(name|version|variables|steps):/gm, '<span class="hl-key">$&</span>')
            .replace(/(\s+)(retry:)/gm, '$1<span class="hl-key">$2</span>')
          } /></pre>
        </div>
        <div class="feat-cards">
          {coreFeatures.map((feat) => (
            <article class="card feat-card reveal-on-scroll">
              <div class="feat-icon" set:html={feat.icon} />
              <h3>{feat.title}</h3>
              <p>{feat.desc}</p>
            </article>
          ))}
        </div>
      </div>
    </section>

    <section class="section" id="secondary-features">
      <div class="section-header reveal-on-scroll">
        <p class="kicker">FEATURES</p>
        <h2>Built for production</h2>
        <p class="section-sub">Everything beyond the basics to run AI workflows at scale.</p>
      </div>
      <div class="card-grid three">
        {secondaryFeatures.map((feat) => (
          <article class="card feat-card reveal-on-scroll">
            <div class="feat-icon" set:html={feat.icon} />
            <h3>{feat.title}</h3>
            <p>{feat.desc}</p>
          </article>
        ))}
      </div>
    </section>

    <section class="section" id="pricing">
      <div class="section-header reveal-on-scroll">
        <p class="kicker">PRICING</p>
        <h2>Upgrade as your usage grows</h2>
        <p class="section-sub">All accounts start on Free by default. Upgrade anytime for higher limits and advanced features.</p>
      </div>
      <div class="pricing-toggle" style="display:flex;justify-content:center;gap:8px;">
        <button type="button" data-billing-toggle="month" class="outline-link" style="padding:8px 14px;">Monthly</button>
        <button type="button" data-billing-toggle="year" class="outline-link" style="padding:8px 14px;">Yearly (save 10%)</button>
      </div>
      <div class="card-grid three">
        {pricing.map((plan) => (
          <article class={`card pricing-card reveal-on-scroll ${plan.featured ? "featured" : ""}`}>
            {plan.featured && <div class="popular-badge">MOST POPULAR</div>}
            <h3>{plan.name}</h3>
            <div class="price-row">
              <span
                class="price"
                data-monthly={plan.monthly}
                data-yearly={plan.yearly}
              >
                {plan.monthly}
              </span>
              {plan.periodMonthly && (
                <span
                  class="price-period"
                  data-monthly={plan.periodMonthly}
                  data-yearly={plan.periodYearly}
                >
                  {plan.periodMonthly}
                </span>
              )}
            </div>
            <p class="price-caption">{plan.caption}</p>
            <div class="price-sep"></div>
            <ul>
              {plan.items.map((item) => (
                <li>{item}</li>
              ))}
            </ul>
            <a
              href={plan.name === "Enterprise" ? "/contact" : `${APP_URL}/register${plan.paid ? `?plan=${plan.name.toLowerCase()}&interval=month` : ""}`}
              data-href-base={plan.name === "Enterprise" ? "/contact" : `${APP_URL}/register`}
              data-plan={plan.paid ? plan.name.toLowerCase() : ""}
              class={plan.featured ? "solid-link" : "outline-link"}
            >
              {plan.cta}
            </a>
          </article>
        ))}
      </div>
    </section>

    <footer class="footer">
      <div class="footer-top">
        <div class="footer-brand">
          <div class="footer-logo-row">
            <div class="logo-icon">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="7" height="7" rx="1.5"/><rect x="14" y="14" width="7" height="7" rx="1.5"/><path d="M7 10v4a3 3 0 0 0 3 3h4"/><circle cx="17.5" cy="6.5" r="3.5"/></svg>
            </div>
            <span class="brand-name">stepIQ</span>
          </div>
          <p class="footer-tagline">Zapier for AI — AI-native, server-side,<br />and model-agnostic.</p>
        </div>
        <div class="footer-links">
          <div class="footer-col">
            <h4>Product</h4>
            <a href="#features">Features</a>
            <a href="#pricing">Pricing</a>
            <a href="#templates">Templates</a>
            <a href="#">Changelog</a>
          </div>
          <div class="footer-col">
            <h4>Developers</h4>
            <a href="/docs">Documentation</a>
            <a href="/docs/api-reference">API Reference</a>
            <a href="#">Status</a>
            <a href="#">GitHub</a>
          </div>
          <div class="footer-col">
            <h4>Company</h4>
            <a href="#">About</a>
            <a href="#">Blog</a>
            <a href="#">Privacy</a>
            <a href="#">Terms</a>
          </div>
        </div>
      </div>
      <div class="footer-divider"></div>
      <div class="footer-bottom">
        <p class="footer-copy">2026 Ligerian Labs. All rights reserved.</p>
        <div class="footer-social">
          <a href="#" aria-label="GitHub">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor"><path d="M12 0C5.37 0 0 5.37 0 12c0 5.31 3.435 9.795 8.205 11.385.6.105.825-.255.825-.57 0-.285-.015-1.23-.015-2.235-3.015.555-3.795-.735-4.035-1.41-.135-.345-.72-1.41-1.23-1.695-.42-.225-1.02-.78-.015-.795.945-.015 1.62.87 1.845 1.23 1.08 1.815 2.805 1.305 3.495.99.105-.78.42-1.305.765-1.605-2.67-.3-5.46-1.335-5.46-5.925 0-1.305.465-2.385 1.23-3.225-.12-.3-.54-1.53.12-3.18 0 0 1.005-.315 3.3 1.23.96-.27 1.98-.405 3-.405s2.04.135 3 .405c2.295-1.56 3.3-1.23 3.3-1.23.66 1.65.24 2.88.12 3.18.765.84 1.23 1.905 1.23 3.225 0 4.605-2.805 5.625-5.475 5.925.435.375.81 1.095.81 2.22 0 1.605-.015 2.895-.015 3.3 0 .315.225.69.825.57A12.02 12.02 0 0024 12c0-6.63-5.37-12-12-12z"/></svg>
          </a>
          <a href="#" aria-label="X / Twitter">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor"><path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/></svg>
          </a>
          <a href="#" aria-label="LinkedIn">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor"><path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433a2.062 2.062 0 01-2.063-2.065 2.064 2.064 0 112.063 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/></svg>
          </a>
        </div>
      </div>
    </footer>
  </main>
</Layout>

<script>
  const syncMotionState = () => {
    const root = document.documentElement;
    const body = document.body;
    const motionDisabled =
      window.matchMedia("(prefers-reduced-motion: reduce)").matches ||
      root.dataset.animations === "off" ||
      root.dataset.motion === "off" ||
      body.dataset.animations === "off" ||
      body.dataset.motion === "off" ||
      body.classList.contains("reduce-motion") ||
      body.classList.contains("motion-disabled");

    body.classList.toggle("motion-enabled", !motionDisabled);
  };

  syncMotionState();

  const motionObserver = new MutationObserver(() => syncMotionState());
  motionObserver.observe(document.documentElement, {
    attributes: true,
    attributeFilter: ["data-animations", "data-motion", "class"],
  });
  motionObserver.observe(document.body, {
    attributes: true,
    attributeFilter: ["data-animations", "data-motion", "class"],
  });
  window.matchMedia("(prefers-reduced-motion: reduce)").addEventListener("change", syncMotionState);

  const toggleButtons =
    document.querySelectorAll<HTMLButtonElement>("[data-billing-toggle]");
  const prices =
    document.querySelectorAll<HTMLElement>("[data-monthly][data-yearly]");
  const ctaLinks =
    document.querySelectorAll<HTMLAnchorElement>("[data-href-base][data-plan]");

  function applyInterval(interval: "month" | "year") {
    for (const el of prices) {
      const value =
        interval === "year"
          ? el.getAttribute("data-yearly")
          : el.getAttribute("data-monthly");
      if (value) el.textContent = value;
    }

    for (const link of ctaLinks) {
      const base = link.getAttribute("data-href-base");
      const plan = link.getAttribute("data-plan");
      if (!base) continue;
      if (!plan) {
        link.setAttribute("href", base);
        continue;
      }
      link.setAttribute("href", `${base}?plan=${plan}&interval=${interval}`);
    }

    for (const button of toggleButtons) {
      const active = button.getAttribute("data-billing-toggle") === interval;
      if (active) {
        button.style.background = "var(--accent)";
        button.style.color = "var(--bg-primary)";
      } else {
        button.style.background = "transparent";
        button.style.color = "var(--text-secondary)";
      }
    }
  }

  for (const button of toggleButtons) {
    button.addEventListener("click", () => {
      const interval = button.getAttribute("data-billing-toggle");
      if (interval === "month" || interval === "year") {
        applyInterval(interval);
      }
    });
  }

  applyInterval("month");

  // Mobile menu
  const mobileNav = document.getElementById("mobile-nav");
  const openBtn = document.getElementById("mobile-menu-open");
  const closeBtn = document.getElementById("mobile-menu-close");

  openBtn?.addEventListener("click", () => {
    mobileNav?.classList.add("open");
    document.body.style.overflow = "hidden";
  });

  closeBtn?.addEventListener("click", () => {
    mobileNav?.classList.remove("open");
    document.body.style.overflow = "";
  });

  // Close mobile nav on link click
  mobileNav?.querySelectorAll("a").forEach((link) => {
    link.addEventListener("click", () => {
      mobileNav.classList.remove("open");
      document.body.style.overflow = "";
    });
  });

  // PostHog CTA tracking
  document.querySelectorAll<HTMLAnchorElement>("a.solid-link, a.ghost-link").forEach((link) => {
    link.addEventListener("click", () => {
      const href = link.getAttribute("href") || "";
      const text = link.textContent?.trim() || "";
      if (typeof (window as any).posthog !== "undefined") {
        const section = link.closest("section")?.id || null;
        const inNav = !!link.closest("nav");
        (window as any).posthog.capture("landing_cta_clicked", {
          cta_text: text,
          cta_href: href,
          cta_location: section || (inNav ? "nav" : "page"),
        });
      }
    });
  });

  // Track pricing plan clicks specifically
  document.querySelectorAll<HTMLAnchorElement>("[data-plan]").forEach((link) => {
    link.addEventListener("click", () => {
      const plan = link.getAttribute("data-plan") || "";
      const href = link.getAttribute("href") || "";
      if (typeof (window as any).posthog !== "undefined") {
        (window as any).posthog.capture("landing_pricing_cta_clicked", {
          plan,
          href,
        });
      }
    });
  });

  // Scroll reveal animations
  const revealNodes = document.querySelectorAll<HTMLElement>(".reveal-on-scroll");
  const observer = new IntersectionObserver(
    (entries) => {
      for (const entry of entries) {
        if (!entry.isIntersecting) continue;
        entry.target.classList.add("is-visible");
        observer.unobserve(entry.target);
      }
    },
    { threshold: 0.15, rootMargin: "0px 0px -8% 0px" },
  );
  revealNodes.forEach((node) => observer.observe(node));
</script>
